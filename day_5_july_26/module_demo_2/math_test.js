import mathObj from './math_util.js'
let { add, sub, mul, cars } = mathObj;

add(20, 10);
sub(20, 10);
mul(20, 10);
console.log(cars)

// import React, { Component } from 'react';
// React is default export and Component is Named export